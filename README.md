its an ai assistant which talks and help 
it performs tasks like opening spotify , chrome , instagram, and etc
it also play the music which you told it for ex by using cmnd hey jarvis play "music name" on spotify app. using key words cmnds are imp like "hey jarvis " and "play on spotify app"
it learns like the facts about you and save it in memory "memory.json " file
it initalize by saying cmnd "hey jarvis"
for stoping it use cmnd "go offlie jarvis"


"STEPS OF USING IT"
FIRSTLY 
install zip code then extract it into file then open the file on vs code 
after tht open terminal use these cmnds to run the program
terminal :
     " pip install winrt-runtime winrt windows.UI.Notifications winrt-windows.UI.Notifications.Management
      pip install SpeechRecognition pyttsx3 pyautogui python-dotenv groq pyAudio pywin32 pygame edge-tts pydub imageio-ffmpeg"
      use these cmnds .

IMP use ur own api key of grop 
paste it into gile .env 
          in the line where its written 
      
